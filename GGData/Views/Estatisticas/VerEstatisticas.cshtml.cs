using Microsoft.AspNetCore.Mvc;
using Microsoft.AspNetCore.Mvc.RazorPages;

namespace GGData.Views.Estatisticas
{
    /// <summary>
    /// Modelo da página Razor para visualizar estatísticas.
    /// Contém a lógica para o método GET da página.
    /// </summary>
    public class VerEstatisticasModel : PageModel
    {
        /// <summary>
        /// Método que é chamado ao aceder à página via GET.
        /// Atualmente não contém lógica, mas pode ser usado para inicializar dados.
        /// </summary>
        public void OnGet()
        {
            // Por agora, método vazio pois não há lógica no carregamento da página.
        }
    }
}
